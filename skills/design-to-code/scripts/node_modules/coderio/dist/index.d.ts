interface WorkspaceStructure {
    root: string;
    app: string;
    process: string;
    reports: string;
    db: string;
    checkpoint: string;
    debug: string;
}

declare enum GraphNode {
    INITIAL = "initial",
    PROCESS = "process",
    VALIDATION = "validation",
    DATA = "data",
    CODE = "code"
}
declare enum ValidationMode {
    CodeOnly = "codeOnly",
    ReportOnly = "reportOnly",
    Full = "full"
}
interface GlobalFigmaInfo {
    thumbnail: string;
}
/**
 * Validation-specific configuration.
 * Controls validation behavior in the workflow.
 */
interface ValidationConfig {
    /**
     * Validation execution mode.
     * - codeOnly: generate component code without validation (only commit)
     * - reportOnly: run a single validation pass and generate a report (no code edits)
     * - full: run iterative actor-critic refinement loop (may edit code + commit markers)
     */
    validationMode?: ValidationMode;
}

/**
 * Figma API Types
 *
 * Type definitions for Figma design data structures.
 * These types represent the data fetched from Figma REST API and used throughout the protocol generation process.
 */
/**
 * Parsed Figma URL information
 */
interface FigmaUrlInfo {
    /** The ID of the Figma file */
    fileId: string | null;
    /** The name of the Figma file */
    name: string;
    /** The ID of the Figma node */
    nodeId: string | null;
    /** The name of the project */
    projectName: string | null;
}
/**
 * RGBA color representation
 */
interface FigmaColor {
    r: number;
    g: number;
    b: number;
    a: number;
}
/**
 * Gradient stop definition
 */
interface FigmaGradientStop {
    color: FigmaColor;
    position: number;
}
/**
 * Figma paint/fill object
 * Supports solid colors, gradients, and images
 */
interface FigmaColorObject {
    blendMode?: string;
    visible?: boolean;
    type: string;
    color?: FigmaColor;
    opacity?: number;
    gradientStops?: FigmaGradientStop[];
    gradientHandlePositions?: {
        x: number;
        y: number;
    }[];
    imageRef?: string;
}
/**
 * Position and size in Figma coordinate space
 */
interface FigmaPositionAndSize {
    x: number;
    y: number;
    width: number;
    height: number;
}
/**
 * Text styling properties from Figma
 */
interface FigmaTextStyle {
    fontFamily: string;
    fontPostScriptName: string;
    fontStyle: string;
    fontWeight: number;
    textAutoResize: string;
    fontSize: number;
    textAlignHorizontal: string;
    textAlignVertical: string;
    letterSpacing: number;
    lineHeightPx: number;
    lineHeightPercent: number;
    lineHeightPercentFontSize: number;
    lineHeightUnit: string;
}
/**
 * Computed CSS styles converted from Figma properties
 * Used for direct style application in generated components
 */
interface CSSStyles {
    position?: string;
    top?: string;
    left?: string;
    width?: string;
    height?: string;
    display?: string;
    flexDirection?: string;
    justifyContent?: string;
    alignItems?: string;
    gap?: string;
    padding?: string;
    paddingLeft?: string;
    paddingRight?: string;
    paddingTop?: string;
    paddingBottom?: string;
    backgroundColor?: string;
    backgroundImage?: string;
    background?: string;
    border?: string;
    borderRadius?: string;
    boxShadow?: string;
    opacity?: string | number;
    backdropFilter?: string;
    color?: string;
    fontSize?: string;
    fontFamily?: string;
    fontWeight?: string;
    lineHeight?: string;
    letterSpacing?: string;
    textAlign?: string;
    overflow?: string;
    [key: string]: string | number | undefined;
}
/**
 * Figma frame/node data structure
 * Represents a complete Figma design node with all its properties and children
 */
interface FigmaFrameInfo {
    id: string;
    name: string;
    type: string;
    url?: string;
    thumbnailUrl?: string;
    visible?: boolean;
    scrollBehavior?: string;
    children?: FigmaFrameInfo[];
    characters?: string;
    frames?: FigmaFrameInfo[];
    blendMode?: string;
    clipsContent?: boolean;
    background?: FigmaColorObject[];
    fills?: FigmaColorObject[];
    strokes?: FigmaColorObject[];
    strokeWeight?: number;
    cornerRadius?: number;
    rectangleCornerRadii?: number[];
    strokeAlign?: string;
    backgroundColor?: FigmaColor;
    absoluteBoundingBox: FigmaPositionAndSize;
    absoluteRenderBounds?: FigmaPositionAndSize;
    constraints?: {
        vertical: string;
        horizontal: string;
    };
    exportSettings?: [
        {
            suffix: string;
            format: FigmaImageFormat;
            constraint: {
                type: string;
                value: number;
            };
        }
    ];
    style?: FigmaTextStyle;
    inlineStyles?: CSSStyles;
    effects?: {
        type: string;
        visible?: boolean;
        radius: number;
        color?: FigmaColor;
        offset?: {
            x: number;
            y: number;
        };
        spread?: number;
    }[];
}
/**
 * Supported Figma image export formats
 */
declare enum FigmaImageFormat {
    PNG = "png",
    JPG = "jpg",
    SVG = "svg",
    PDF = "pdf",
    EPS = "eps",
    WEBP = "webp"
}

/**
 * Protocol Structure
 *
 * The protocol is the intermediate representation (IR) between Figma design and generated code.
 * It contains component hierarchy, semantic information, layout data, and reusable component definitions.
 */
interface Protocol {
    /** Unique component identifier (e.g., "Header", "ProductCard") */
    id: string;
    /** Component data including name, metadata, structure, and rendering config */
    data: FrameData;
    /** Nested child components */
    children?: Protocol[] | null;
}
/**
 * Component data container
 * Contains all information needed to generate a component:
 * - Semantic metadata (name, purpose)
 * - Original Figma node data (elements)
 * - Layout and positioning information
 * - Reusable component configuration (props, states)
 *
 * @example for a task list component
 * ```typescript
 * const frameData: FrameData = {
 *   name: "TaskList",
 *   kebabName: "task-list",
 *   path: "components/task-list",
 *   purpose: "Display a list of tasks",
 *   elements: [...],
 *   layout: {...},
 *   states: [
 *     {
 *       state: [
 *         { title: "Task 1", status: "In Progress" },
 *         { title: "Task 2", status: "Completed" }
 *       ],
 *       componentName: "TaskCard",
 *       componentPath: "components/task-card"
 *     }
 *   ]
 * };
 * ```
 *
 * @example for a task card component
 * ```typescript
 * const frameData: FrameData = {
 *   name: "TaskCard",
 *   kebabName: "task-card",
 *   path: "components/task-card",
 *   purpose: "Display a task item with status and assignee",
 *   componentName: "TaskCard",
 *   componentPath: "components/task-card",
 *   elements: [...],
 *   layout: {...},
 *   props: [
 *     { key: "title", type: "string", description: "Task title" },
 *     { key: "status", type: "string", description: "Task status" },
 *     { key: "assignee", type: "string", description: "Task assignee" },
 *   ],
 * };
 * ```
 */
interface FrameData {
    /** Component name (e.g., "Header", "Hero Section") */
    name: string;
    /** Semantic description of component's purpose and functionality */
    purpose: string;
    /** Normalized identifier for filesystem-friendly names (e.g., "task-card") */
    kebabName?: string;
    /** Derived slug for file system paths (e.g., "components/task-card") */
    path?: string;
    /** Complete Figma node data with hierarchy (simplified for AI processing) */
    elements: FigmaFrameInfo[];
    /** Pre-computed layout information (position, size, spacing, direction) */
    layout?: EmbeddedLayoutInfo;
    /**
     * Optional reusable component identifier.
     * When present, this node is an instance of a reusable component (e.g. "TaskCard", "FeatureCard").
     */
    componentName?: string;
    /** Derived slug for component file system paths */
    componentPath?: string;
    /**
     * Props schema definition for reusable component template.
     * Defines the formal parameters (key, type, description) for the component.
     */
    props?: Array<{
        key: string;
        type: string;
        description: string;
    }>;
    /**
     * Array of component states with their data lists.
     * Each entry contains: state array (actual parameter values), componentName, and componentPath.
     */
    states?: Array<{
        state: Array<Record<string, unknown>>;
        componentName: string;
        componentPath: string;
    }>;
}
/**
 * Embedded layout information
 * Direct layout data attached to each node in structure tree
 * Eliminates need for separate LayoutMeasurementProvider lookups
 */
interface EmbeddedLayoutInfo {
    /** Position and size in CSS coordinates */
    boundingBox: CssBoundingBox;
    /** Position and size relative to parent component */
    relativeBoundingBox?: CssBoundingBox;
    /** Computed layout direction based on children positions */
    layoutDirection: string;
    /** Gap to previous and next siblings */
    spacing: {
        previous?: number;
        next?: number;
    };
    /** Pre-computed offset from parent's bounding box */
    parentRelativeOffset: {
        x: number;
        y: number;
    };
}
/**
 * Bounding box with position and size using CSS coordinate naming (top, left)
 */
interface CssBoundingBox {
    top: number;
    left: number;
    width: number;
    height: number;
}

/**
 * Shared validation types.
 *
 * Types used across multiple subsystems in the validation pipeline:
 * - Validation node (src/nodes/validation/)
 * - Position tool (src/tools/position-tool/)
 * - Report tool (src/tools/report-tool/)
 * - History tool (src/tools/history-tool/)
 * - Judger agent (src/agents/judger-agent/)
 *
 * Per type organization hierarchy: shared types go in src/types/
 */

/**
 * Generic dictionary/object type with string keys and unknown values.
 * Used for flexible data structures like Figma JSON trees, structure trees, etc.
 */
type Dict = Record<string, unknown>;
/**
 * Figma position with offset already applied (CSS coordinates).
 */
interface FigmaPosition {
    x: number;
    y: number;
    w: number;
    h: number;
}
/**
 * Component information from protocol structure.
 */
interface ComponentInfo {
    id: string;
    name: string;
    path: string;
}
/**
 * Complete metadata for a single element (Figma node) in context.
 */
interface ElementInfo {
    id: string;
    name: string;
    type: string;
    /** Position with offset already applied (CSS coordinates) */
    position: FigmaPosition;
    /** Raw Figma layout properties */
    layoutMode?: string;
    primaryAxisAlignItems?: string;
    counterAxisAlignItems?: string;
    itemSpacing?: number;
    paddingTop?: number;
    paddingRight?: number;
    paddingBottom?: number;
    paddingLeft?: number;
    constraints?: Record<string, unknown>;
    /** Parent component ID */
    parentComponentId: string;
    /** Parent component name */
    parentComponentName: string;
    /** Parent component path */
    parentComponentPath: string;
    /** Parent item type */
    parentItemType: 'frame' | 'component';
}
/**
 * Extended Figma frame information for validation.
 * Includes layout properties needed for position validation.
 */
interface ValidationFrameInfo extends FigmaFrameInfo {
    layoutMode?: string;
    primaryAxisAlignItems?: string;
    counterAxisAlignItems?: string;
    itemSpacing?: number;
    paddingTop?: number;
    paddingRight?: number;
    paddingBottom?: number;
    paddingLeft?: number;
}
/**
 * Unified validation context extracted from protocol.
 * Built once at validation start and reused across all iterations.
 */
interface ValidationContext {
    /** Coordinate offset from Figma canvas to CSS (0,0) origin */
    offset: {
        x: number;
        y: number;
    };
    /** All elements indexed by Figma node ID */
    elements: Map<string, ElementInfo>;
    /** All components indexed by component ID */
    components: Map<string, ComponentInfo>;
    /** Element ID to parent component mapping (for compatibility) */
    elementToComponent: Map<string, ComponentInfo>;
    /** All Figma positions indexed by node ID (offset already applied) */
    figmaPositions: Record<string, FigmaPosition | undefined>;
}
/**
 * Element metadata registry for compatibility with existing APIs.
 */
interface ElementMetadataRegistry {
    elements: Map<string, {
        id: string;
        name: string;
        parentComponentId: string;
        parentComponentName: string;
        parentComponentPath: string;
        parentItemType: 'frame' | 'component';
    }>;
    components: Map<string, ComponentInfo>;
}
/**
 * User-facing validation report with annotated screenshots and analysis
 * Generated by ValidationNode after position capture and annotation
 */
interface UserReport {
    design: {
        snap: string;
        markedSnap: string;
    };
    page: {
        url: string;
        snap: string;
        markedSnap: string;
    };
    report: {
        heatmap: string;
        detail: {
            metrics: {
                mae: number;
                sae: number;
                misalignedCount: number;
            };
            components: {
                componentId: string;
                componentPath: string;
                elementId?: string;
                validationInfo?: {
                    x: number;
                    y: number;
                };
                elements?: {
                    elementId: string;
                    elementIndex: number;
                    validationInfo: {
                        x: number;
                        y: number;
                    };
                }[];
            }[];
        };
    };
}
/**
 * Structured validation error report
 * Contains position comparison data for a single component
 */
interface ValidationReport {
    /** [x, y] of rendered position */
    currentPosition: [number, number];
    /** [x, y] of expected Figma position */
    targetPosition: [number, number];
    /** [abs_error_x, abs_error_y] absolute values of position error */
    absoluteError: [number, number];
}
/**
 * Misaligned component data
 * Contains all information about a component that needs fixing
 */
interface MisalignedComponent {
    name: string;
    componentId: string;
    elementIds: string[];
    path: string;
    validationReport: ValidationReport;
    currentX: number;
    currentY: number;
    targetX: number;
    targetY: number;
    currentWidth: number;
    currentHeight: number;
    targetWidth: number;
    targetHeight: number;
    distance: number;
}
/**
 * Component history entry
 * Tracks position and fix across iterations for HistoryTool
 */
interface ComponentHistoryEntry {
    iteration: number;
    position: [number, number];
    error: [number, number];
    fixApplied: string[] | null;
}
/**
 * Component history map
 * Maps component IDs to their history entries
 */
type ComponentHistory = Record<string, ComponentHistoryEntry[]>;

/**
 * Simplified Figma node structure for content extraction.
 */
interface SimplifiedFigmaNode {
    id: string;
    name: string;
    type: string;
    url?: string;
    cornerRadius?: number;
    characters?: string;
    visible?: boolean;
    absoluteBoundingBox?: FigmaPositionAndSize;
    children?: SimplifiedFigmaNode[];
    inlineStyles?: Record<string, unknown>;
    componentProperties?: Record<string, unknown>;
    style?: Record<string, unknown>;
    hasStrokes?: boolean;
}
/**
 * Extended node type with potential top-level componentName (non-standard AI format).
 */
interface ExtendedFrameStructNode extends Protocol {
    componentName?: string;
}
/**
 * Parsed data list response from AI model.
 */
interface ParsedDataListResponse {
    state?: Array<Record<string, unknown>>;
    props?: Array<{
        key: string;
        type: string;
        description: string;
    }>;
}

/**
 * Generates the system prompt for the InitialAgent.
 * Defines project requirements, tech stack (React + Tailwind V4), and safety constraints.
 *
 * @param options - Configuration options for the prompt generation.
 * @param options.appPath - The target directory path where the project will be scaffolded.
 */
declare const INITIAL_AGENT_SYSTEM_PROMPT: string;

declare function initialAgentInstruction(params: {
    appPath: string;
    appName: string;
}): string;

/**
 * Parse Figma URL and extract fileId, name, and nodeId in one pass
 * @param url - Figma URL to parse
 * @returns Object containing fileId, name, and nodeId
 * @example
 * parseFigmaUrl('https://www.figma.com/design/aONcu8L82l1PdcT304Q8Za/Intern?node-id=0-495')
 * // Returns: { fileId: 'aONcu8L82l1PdcT304Q8Za', name: 'intern', nodeId: '0-495' }
 */
declare const parseFigmaUrl: (url: string) => FigmaUrlInfo;

interface ImageNode {
    id: string;
    name: string;
    format: FigmaImageFormat;
    url?: string;
    success?: boolean;
    remote?: string;
}

/**
 * Executes the Figma and images actions.
 * @param state - The state of the graph.
 * @returns The state of the graph with imageNodesMap as Map<string, ImageNode>.
 */
declare const executeFigmaAndImagesActions: (urlInfo: FigmaUrlInfo, assetsDir: string, processDir: string) => Promise<{
    document: FigmaFrameInfo;
    imageNodesMap: Map<string, ImageNode>;
}>;

declare class FigmaTool {
    fetchAndClean(fileId: string, nodeId: string, token: string): Promise<FigmaFrameInfo | undefined>;
    downloadImages(fileId: string, token: string, imageDir: string, document?: FigmaFrameInfo): Promise<{
        successCount: number;
        failCount: number;
        imageNodesMap: Map<string, ImageNode>;
    }>;
    simplifyImageNodes(node: FigmaFrameInfo, imageNodes: Map<string, ImageNode>): FigmaFrameInfo;
    processedStyle(node: FigmaFrameInfo): FigmaFrameInfo;
}
declare const figmaTool: FigmaTool;

/**
 * Simplifies Figma nodes for content extraction, retaining essential fields for AI processing
 * Removes heavy vector data while keeping text content, images, and layout information
 *
 * @param node - The Figma frame node to simplify
 * @returns Simplified node with only essential fields
 */
declare function simplifyFigmaNodeForContent(node: FigmaFrameInfo): SimplifiedFigmaNode;
/**
 * Extract node positions with hierarchical structure preserved
 * Returns nested position data maintaining parent-child relationships
 *
 * @param data - Figma frame data (single frame or array of frames)
 * @returns Hierarchical position data with node information
 */
declare function extractNodePositionsHierarchical(data: FigmaFrameInfo[] | FigmaFrameInfo | undefined): Record<string, unknown>;
/**
 * Extract nodes by IDs while preserving hierarchical structure
 * Nodes in idList are kept; if a deep child is in idList but parent isn't, the child is still extracted
 *
 * @param tree - The Figma frame tree to search
 * @param idList - Array of node IDs to extract
 * @param options - Optional settings
 * @param options.includeSubtree - If true, includes all descendants of matched nodes
 * @returns Array of extracted nodes with hierarchy preserved
 */
declare function extractHierarchicalNodesByIds(tree: FigmaFrameInfo | FigmaFrameInfo[], idList: string[], options?: {
    includeSubtree?: boolean;
}): FigmaFrameInfo[];
/**
 * Post-processes the structure tree in a single traversal
 * Performs three operations simultaneously:
 * 1. Normalizes componentName (moves from top-level to data field)
 * 2. Populates elements data from elementIds
 * 3. Annotates with file system paths (path, componentPath, kebabName)
 *
 * @param structure - The parsed structure from AI model
 * @param frames - The Figma frames tree for element extraction
 */
declare function postProcessStructure(structure?: Protocol | Protocol[] | null, frames?: FigmaFrameInfo[]): void;
/**
 * Extract component groups from protocol children
 * Groups components by their componentName for batch processing
 *
 * @param protocol - The protocol node to analyze
 * @returns Map of componentName to array of instances
 */
declare function extractComponentGroups(node: Protocol): Map<string, Protocol[]>;
/**
 * Applies props and state to the protocol node
 * @param parsed - The parsed data list response
 * @param node - The protocol node to apply the props and state to
 * @param compName - The name of the component
 * @param group - The group of components
 * @param isList - Whether the component is a list
 */
declare function applyPropsAndStateToProtocol(parsed: ParsedDataListResponse, node: Protocol, compName: string, group: Protocol[], isList: boolean): void;

/**
 * Prompt template for structure generation.
 * Matches the style used in agents/initial-agent/prompt.ts.
 */
declare const generateStructurePrompt: (options: {
    positions: string;
    width?: string;
}) => string;
/**
 * Prompt template for extracting data list and props schema.
 */
declare const extractDataListPrompt: (options: {
    containerName: string;
    childComponentName: string;
    figmaData: string;
}) => string;

/**
 * Response parsing utilities for AI model outputs.
 * Handles extraction of structured content from markdown-formatted responses.
 */

/**
 * Extract JSON content from markdown code blocks
 * Handles cases where AI models wrap JSON in ```json ... ``` or ``` ... ```
 * If no code block markers are found, returns the original content
 *
 * @param response - Model response that may contain markdown code blocks
 * @returns Extracted JSON string without markdown formatting
 *
 * @example
 * // Input: "```json\n{\"key\": \"value\"}\n```"
 * // Output: "{\"key\": \"value\"}"
 */
declare function extractJSON(response: string): string;

/**
 * Flatten tree into array using post-order traversal
 */
declare function flattenPostOrder(node: Protocol): Protocol[];
/**
 * Detect which rendering modes are used in this frame
 */
declare function detectRenderingModes(node: Protocol): {
    hasStates: boolean;
    hasIndependentChildren: boolean;
};
/**
 * Helper function to save generated code (handles both single and multi-file output)
 */
declare function saveGeneratedCode(code: string, filePath: string): void;

declare const DEFAULT_STYLING: {
    approach: string;
    libraries: {
        name: string;
        role: string;
    }[];
};

declare const generateFramePrompt: ({ childrenImports, frameDetails, assetFiles, styling, renderingModes, }: {
    childrenImports: string;
    frameDetails: string;
    assetFiles?: string;
    styling: string;
    renderingModes: {
        hasStates: boolean;
        hasIndependentChildren: boolean;
    };
}) => string;
declare const generateComponentPrompt: ({ componentName, componentDetails, styling, assetFiles, }: {
    componentName: string;
    componentDetails: string;
    styling: string;
    assetFiles?: string;
}) => string;

/**
 * Defines the logical structure of the output workspace.
 * output directory structure
    coderio/
    └── figmaName/                   # Project root directory generated from a Figma URL
        ├── my-app/                  # Generated project source code
        ├── process/                 # Intermediate data and cache during generation
        │   ├── validation/          # Validation reports, screenshots, and processed.json
        │   └── ...                  # Other process artifacts
        ├── reports.html             # Validation reports summary
        └── checkpoint/              # Cache
            ├── coderio-cli.db
            └── checkpoint.json
*/
declare class Workspace {
    path: WorkspaceStructure | null;
    initWorkspace(subPath: string, rootPath?: string, appName?: string): WorkspaceStructure;
    /**
     * Delete all files and directories inside the workspace
     * @param workspace - The workspace structure
     * @param preserve - Optional list of relative paths (from workspace root) to preserve from deletion
     */
    deleteWorkspace(workspace: WorkspaceStructure, preserve?: string[]): void;
    /**
     * Resolve the absolute path to the source code directory
     * @param paths - The workspace structure
     * @param srcSubPath - The subpath to the source code directory
     * @returns The absolute path to the source code directory
     */
    resolveAppSrc(paths: WorkspaceStructure, srcSubPath: string): string;
    /**
     * Resolve component alias path to absolute filesystem path.
     *
     * Handles various path formats:
     * - @/components/Button → /workspace/my-app/src/components/Button/index.tsx
     * - @/src/components/Button → /workspace/my-app/src/components/Button/index.tsx
     * - components/Button → /workspace/my-app/src/components/Button/index.tsx
     *
     */
    resolveComponentPath(aliasPath: string): string;
}
declare const workspaceManager: Workspace;

/**
 * Download an image from URL and save to local directory
 * @param url - Image URL to download
 * @param filename - Local filename (with extension)
 * @param outputDir - Output directory path
 * @returns Local file path
 */
declare function downloadImage(url: string, filename?: string, imageDir?: string, base64?: boolean): Promise<string>;

/**
 * Standard naming utilities for the project.
 * Ensures consistency between kebab-case file paths and PascalCase component names.
 */
/**
 * Converts a string to PascalCase (e.g. "my-component" -> "MyComponent")
 * Used for Component Names and imports.
 */
declare function toPascalCase(str: string): string;
/**
 * Converts a string to kebab-case (e.g. "MyComponent" -> "my-component")
 * Used for file paths and directories.
 */
declare function toKebabCase(str: string): string;

export { type CSSStyles, type ComponentHistory, type ComponentHistoryEntry, type ComponentInfo, DEFAULT_STYLING, type Dict, type ElementInfo, type ElementMetadataRegistry, type ExtendedFrameStructNode, type FigmaColor, type FigmaColorObject, type FigmaFrameInfo, type FigmaGradientStop, FigmaImageFormat, type FigmaPosition, type FigmaPositionAndSize, type FigmaTextStyle, type FigmaUrlInfo, type FrameData, type GlobalFigmaInfo, GraphNode, INITIAL_AGENT_SYSTEM_PROMPT, type MisalignedComponent, type ParsedDataListResponse, type Protocol, type SimplifiedFigmaNode, type UserReport, type ValidationConfig, type ValidationContext, type ValidationFrameInfo, ValidationMode, type ValidationReport, type WorkspaceStructure, applyPropsAndStateToProtocol, detectRenderingModes, downloadImage, executeFigmaAndImagesActions, extractComponentGroups, extractDataListPrompt, extractHierarchicalNodesByIds, extractJSON, extractNodePositionsHierarchical, figmaTool, flattenPostOrder, generateComponentPrompt, generateFramePrompt, generateStructurePrompt, initialAgentInstruction, parseFigmaUrl, postProcessStructure, saveGeneratedCode, simplifyFigmaNodeForContent, toKebabCase, toPascalCase, workspaceManager };
